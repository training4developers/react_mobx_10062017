import React from 'react';

export const ToolHeader = props => {
  
  //throw Error('the world has ended because Larry\'s screen will not share properly');

  return <header>
    <h1>{props.headerText}</h1>
  </header>;

};